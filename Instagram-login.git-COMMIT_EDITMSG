Assistant checkpoint: Updated database configuration to use SQLite

Assistant generated file changes:
- app.py: Update database configuration to use SQLite

---

User prompt:

The app isn't running, review and fix errors in the Console
